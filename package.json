{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "deploy-storybook": "storybook-to-ghpages",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.5.6"
  }
}
